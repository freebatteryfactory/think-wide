# TEMPLATE ONLY. Copy to shared/app.env.local (0600), supply values privately.
# Empty credentials are intentional and must be replaced before check-connected.
WORKOS_API_KEY=
WORKOS_CLIENT_ID=
WORKOS_COOKIE_PASSWORD=
WORKOS_REDIRECT_URI=https://think-wide.fbf.systems/api/auth/callback
THINK_WIDE_IDENTITY=workos
# T08 HTTP resource metadata; does not configure an OAuth authorization server.
MCP_RESOURCE_URL=https://think-wide.fbf.systems/api/mcp
# Set only after observing the real MCP OAuth token/discovery profile.
MCP_AUTHORIZATION_SERVER=
